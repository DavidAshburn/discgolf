<br><br>
<div class="card w-80">
  <h1 class="card-header text-center"><%= @course.name %></h5>
  <div class="card-body">
    <h5 class="card-title text-center text-muted"><%= " #{@course.city}, #{@course.state} " %></h5>

    <table class="table table-striped table-hover table-sm">
      <thead scope="row" style="background-color: #11d47c;">
        <tr class="fs-5">
          <th scope="col" class="text-white text-center">Variant</th>
          <th scope="col" class="text-white">1</th>
          <th scope="col" class="text-white">2</th>
          <th scope="col" class="text-white">3</th>
          <th scope="col" class="text-white">4</th>
          <th scope="col" class="text-white">5</th>
          <th scope="col" class="text-white">6</th>
          <th scope="col" class="text-white">7</th>
          <th scope="col" class="text-white">8</th>
          <th scope="col" class="text-white">9</th>           
        </tr>
      </thead>
      <tbody>
      <% @course.variants.each do |variant| %>
        <tr class="align-middle fs-4">
          <td class="align-middle"><%= link_to variant.name, edit_variant_path(variant.id, :course_id => @course.id), class:"btn btn-success" %></td>
          <td><%= variant.one %></td>
          <td><%= variant.two %></td>
          <td><%= variant.three %></td>
          <td><%= variant.four %></td>
          <td><%= variant.five %></td>
          <td><%= variant.six %></td>
          <td><%= variant.seven %></td>
          <td><%= variant.eight %></td>
          <td><%= variant.nine %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
    <br>

    <div class="row">
      <div class="d-grid gap-2">
        <%= link_to "Add a Variant", new_variant_path(:course_id => @course.id), class:"btn btn-success mb-3" %>
        <%= link_to "Edit this course", edit_course_path, class:"btn btn-warning mb-3" %>
        <%= link_to "Course list", courses_path, class:"btn btn-outline-dark mb-3" %>
      </div>
      <div class="col align-self-end">
        <%= button_to "Delete", @course, method: :delete, class:"btn btn-outline-danger" %>
      </div>
    </div>
  </div>
</div>